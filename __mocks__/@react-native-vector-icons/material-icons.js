const React = require('react');
const { Text } = require('react-native');

const MaterialIcons = (props) => React.createElement(Text, props, '');
MaterialIcons.displayName = 'MaterialIcons';

module.exports = MaterialIcons;
